I now want to implement a use case of my puppys agent framwork. The test case is to let the agent to play ‘room escape’.

Help me using python to implement a game UI for the room escape:
The game map is just a square grid, the dimension can be set. For example, if dimension is 5, then the game map is the 5*5 grid.
Each square can be empty (path), obstacle (wall), doors (the exit location), useful tools, confusing tools, and where the agent is currently located, each using different representations.
The agent is the game character, so can be controlled to turn, move, stop, and act. The act here means to interact with the tools, either useful tools or confusing tools. The actions can be get the info of the tool and aquire or leave the tool.
The tools' location and meanings are all randomly generate, but have to contain at least one useful tool. The agent's goal is set to be fixed, which is to escape the room from the doors. However, the agent cannot just go out by standing in front of the door, instead, the agent need to use tools like key to open the door and then get out. The right key is the useful tool, but can also add confusing tools like wrong keys, or stones, or cash, etc. that are useless to get out of the room. The useful tool can be anything, the key is just an example. When defining the tool, need to have a name, description, and the guide to use it, the tool can be anything regardless the name, as long as it fit with the desciprion and the usage guide. The location is also randomly generated, but have to make sure that the location is avaialble, cannot have multiple things in one location. The location is then sent to the UI and display the tool somehow in the UI. 
When controlling the agent, make sure the agent cannot go beyond the boundary and stand in the location where there is something else. The map should display and label all the things inside the map, like the tools, walls, doors, etc. When the agent is in front of a tool, make sure to display the controllable componets in the UI. For example, when the agent is in front of the key, the name, description, and usage guide should be displayed somewhere in the UI. Then, can use keys or mouse clicks to control the agent do the next step, like keep moving or act with the tool. The acts for each tool is predefinded in the code. For example, can define the method pick up, drop down, lock the door, unlock the door for the tool key. Then there should be the corresponding control machanisms for each action in the UI. Note that, the actions can be anything, so need to think of an efficient and robust method to unify and classicify the action control machism, otherwise will be too massy and hard to control and remember all the different machanisms.
In the UI, the current action have to be display at the bottom, for example, when the agent is moving taking the key, in the UI, there should be text 'the agent is now moving up, using the tool right key, taking the key when moving'. The result of the action should also be displayed. For example, when the agent is unlocking the door. There should be text like 'Trying to unlock the door...' and a few moments later, 'unlock successfully.'. The text is for both the human and the agent to know what is going on in the current action.

Note: This is the first step of the use case, so you do not need to worry about the puppys framework, just implement the game UI and the control logic. Make sure the main character can now be controlled by human.

The game HAS TO BE run with a beautiful graphical interface with a cool style and lots of icons, not the classical style game interface, NOT the command line.

The above is the detailed description of the game implementation requirements. Analysis the requirements in depth, think step by step, and write out high-quality python code to implement all the requirements. Make sure the code is highly OO and robust and easy to use, while make sure everything fit the requirements and the code can be run without and potential bugs.